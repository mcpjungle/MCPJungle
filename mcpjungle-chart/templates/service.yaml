apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcpjungle.fullname" . }}
  labels:
    {{- include "mcpjungle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcpjungle.selectorLabels" . | nindent 4 }}
